import type{PoolClient}from"pg";import{getFinancialPool}from"./pool";export async function withFinancialTransaction<T>(fn:(client:PoolClient)=>Promise<T>){const pool=getFinancialPool();if(!pool)throw Error("FINANCIAL_DATABASE_CONFIGURATION_REQUIRED");const client=await pool.connect();try{await client.query("BEGIN");const result=await fn(client);await client.query("COMMIT");return result}catch(error){await client.query("ROLLBACK");throw error}finally{client.release()}}
