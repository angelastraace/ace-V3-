import P from"../../PlatformShell";import s from"../../PlatformShell.module.css";import{financialHealth}from"../../../lib/financial-health";export const dynamic="force-dynamic";export default async function Page(){const h=await financialHealth();return <P><main className={s.main}><div className={s.kicker}>ADMIN / V4A FOUNDATION</div><h1>Financial<br/><span>System.</span></h1><div className={s.grid}>{Object.entries(h).filter(([k])=>k!=="status").map(([k,v])=><article className={s.card} key={k}><small>{k}</small><h3>{v?"HEALTHY":"CONFIGURATION REQUIRED"}</h3><p>No financial execution is available.</p></article>)}</div></main></P>}
