import PlatformShell from "../PlatformShell";import AuthRecoveryForm from "../AuthRecoveryForm";import styles from "../PlatformShell.module.css";
export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;return <PlatformShell><main className={styles.main}><div className={styles.kicker}>ACE IDENTITY</div><section className={styles.hero}><div><h1>Reset<br/><span>Password.</span></h1><p>Reset tokens are validated by the configured production identity provider.</p></div></section><section className={styles.section}>{token?<AuthRecoveryForm mode="reset-password" token={token}/>:<div className={styles.notice}>A reset token is required. Request a new password-reset email first.</div>}</section></main></PlatformShell>}
