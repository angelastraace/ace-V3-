import { NextResponse } from "next/server";import { providerProxy } from "../../../../lib/proxy";import { featureFlags, providerConfig } from "../../../../lib/config";import { enforceRateLimit } from "../../../../lib/rate-limit";
export async function POST(req:Request){if(!featureFlags.governanceVoting||!providerConfig.governance)return NextResponse.json({status:"blocked",reason:"Governance voting is not enabled"},{status:503});const limited=await enforceRateLimit(req,"governance:vote");if(limited)return limited;return providerProxy(req,process.env.GOVERNANCE_API_URL,process.env.GOVERNANCE_API_TOKEN,"votes",{allowMethods:["POST"],requireIdempotency:true,requireSameOrigin:true});}
