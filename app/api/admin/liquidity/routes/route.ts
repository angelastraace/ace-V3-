import { NextResponse } from "next/server";import { simulator } from "../../../../../lib/liquidity-router";import { requireInternalStatusAccess } from "../../../../../lib/internal-access";export function GET(req:Request){const denied=requireInternalStatusAccess(req);if(denied)return denied;return NextResponse.json(simulator().router.quote("USDT",100,[{asset:"USDC",amount:100,usdValue:100}]))}
