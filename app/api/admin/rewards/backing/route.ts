import { NextResponse } from "next/server";import { simulator } from "../../../../../lib/liquidity-router";import { requireInternalStatusAccess } from "../../../../../lib/internal-access";export function GET(req:Request){const denied=requireInternalStatusAccess(req);if(denied)return denied;return NextResponse.json({status:"simulation",backing:simulator().funding.sources(),executionEnabled:false})}
