import{NextResponse}from"next/server";import{requireInternalStatusAccess}from"../../../../../../lib/internal-access";import{fundingEngine}from"../../../../../../lib/reward-funding";export async function POST(req:Request){const d=requireInternalStatusAccess(req);if(d)return d;const b=await req.json().catch(()=>null) as {name?:string;type?:"PROTOCOL_REVENUE"|"SPONSOR"|"PARTNER"|"TREASURY"|"CREDIT";approvedAmount?:number;expiresAt?:string}|null;try{return NextResponse.json(fundingEngine.createSource({name:b?.name||"",type:b?.type||"SPONSOR",approvedAmount:b?.approvedAmount||0,expiresAt:b?.expiresAt},req.headers.get("idempotency-key")||""))}catch(e){return NextResponse.json({status:"blocked",reason:e instanceof Error?e.message:"INVALID"},{status:422})}}
