import { NextResponse } from "next/server";import { providerProxy } from "../../../../../lib/proxy";import { featureFlags, financialCoreReady, publicLaunchApproved } from "../../../../../lib/config";import { enforceRateLimit } from "../../../../../lib/rate-limit";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!/^[A-Za-z0-9_-]{1,128}$/.test(id))return NextResponse.json({status:"invalid_request"},{status:400});if(!featureFlags.trading||!financialCoreReady()||!publicLaunchApproved())return NextResponse.json({status:"blocked",reason:"Trading is not production-enabled"},{status:503});const limited=await enforceRateLimit(req,"finance:order-cancel");if(limited)return limited;return providerProxy(req,process.env.TRADING_EXECUTION_URL,process.env.TRADING_EXECUTION_TOKEN,`orders/${encodeURIComponent(id)}/cancel`,{allowMethods:["POST"],requireIdempotency:true,requireSameOrigin:true});}
