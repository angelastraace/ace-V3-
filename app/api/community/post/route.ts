import { NextResponse } from "next/server";import { providerProxy } from "../../../../lib/proxy";import { featureFlags, providerConfig } from "../../../../lib/config";import { enforceRateLimit } from "../../../../lib/rate-limit";
export async function POST(req:Request){if(!featureFlags.communityWrites||!providerConfig.community)return NextResponse.json({status:"blocked",reason:"Community posting is not enabled"},{status:503});const limited=await enforceRateLimit(req,"community:post");if(limited)return limited;return providerProxy(req,process.env.COMMUNITY_API_URL,process.env.COMMUNITY_API_TOKEN,"posts",{allowMethods:["POST"],requireIdempotency:true,requireSameOrigin:true});}
