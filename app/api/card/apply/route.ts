import { NextResponse } from "next/server";import { providerProxy } from "../../../../lib/proxy";import { featureFlags, providerConfig, publicLaunchApproved } from "../../../../lib/config";import { enforceRateLimit } from "../../../../lib/rate-limit";
export async function POST(req:Request){if(!featureFlags.card||!providerConfig.card||!providerConfig.kyc||!providerConfig.aml||!publicLaunchApproved())return NextResponse.json({status:"blocked",reason:"ACE Card application is not production-enabled"},{status:503});const limited=await enforceRateLimit(req,"card:apply");if(limited)return limited;return providerProxy(req,process.env.CARD_PROVIDER_URL,process.env.CARD_PROVIDER_KEY,"applications",{allowMethods:["POST"],requireIdempotency:true,requireSameOrigin:true});}
