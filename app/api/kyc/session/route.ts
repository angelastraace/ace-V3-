import { NextResponse } from "next/server";import { providerProxy } from "../../../../lib/proxy";import { providerConfig } from "../../../../lib/config";import { enforceRateLimit } from "../../../../lib/rate-limit";
export async function POST(req:Request){if(!providerConfig.kyc)return NextResponse.json({status:"blocked",reason:"KYC provider not configured"},{status:503});const limited=await enforceRateLimit(req,"identity:kyc-session");if(limited)return limited;return providerProxy(req,process.env.KYC_PROVIDER_URL,process.env.KYC_PROVIDER_KEY,"session",{allowMethods:["POST"],requireSameOrigin:true});}
