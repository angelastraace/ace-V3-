import { NextResponse } from "next/server";import { providerProxy } from "../../../../lib/proxy";import { featureFlags, providerConfig, publicLaunchApproved } from "../../../../lib/config";import { enforceRateLimit } from "../../../../lib/rate-limit";
export async function POST(req:Request){if(!featureFlags.creatorPayouts||!providerConfig.creator||!providerConfig.kyc||!providerConfig.aml||!publicLaunchApproved())return NextResponse.json({status:"blocked",reason:"Creator payouts are not production-enabled"},{status:503});const limited=await enforceRateLimit(req,"creator:payout");if(limited)return limited;return providerProxy(req,process.env.CREATOR_API_URL,process.env.CREATOR_API_TOKEN,"payouts",{allowMethods:["POST"],requireIdempotency:true,requireSameOrigin:true});}
